<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="390" onload="init(evt)" viewBox="0 0 1200 390" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="390" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="373.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="373.00"> </text><svg id="frames" x="10" width="1180" total_samples="56085"><g><title>`0x7FFD5619B684 (7 samples, 0.01%)</title><rect x="0.0089%" y="261" width="0.0125%" height="15" fill="rgb(227,0,7)" fg:x="5" fg:w="7"/><text x="0.2589%" y="271.50"></text></g><g><title>`0x7FFD56115D80 (7 samples, 0.01%)</title><rect x="0.0089%" y="245" width="0.0125%" height="15" fill="rgb(217,0,24)" fg:x="5" fg:w="7"/><text x="0.2589%" y="255.50"></text></g><g><title>`0x7FFD560C84E0 (7 samples, 0.01%)</title><rect x="0.0089%" y="229" width="0.0125%" height="15" fill="rgb(221,193,54)" fg:x="5" fg:w="7"/><text x="0.2589%" y="239.50"></text></g><g><title>`0x7FFD5612548B (9 samples, 0.02%)</title><rect x="0.0089%" y="277" width="0.0160%" height="15" fill="rgb(248,212,6)" fg:x="5" fg:w="9"/><text x="0.2589%" y="287.50"></text></g><g><title>`0x7FFD560F5FCE (20 samples, 0.04%)</title><rect x="0.0000%" y="325" width="0.0357%" height="15" fill="rgb(208,68,35)" fg:x="0" fg:w="20"/><text x="0.2500%" y="335.50"></text></g><g><title>`0x7FFD5612364E (20 samples, 0.04%)</title><rect x="0.0000%" y="309" width="0.0357%" height="15" fill="rgb(232,128,0)" fg:x="0" fg:w="20"/><text x="0.2500%" y="319.50"></text></g><g><title>`0x7FFD56123864 (20 samples, 0.04%)</title><rect x="0.0000%" y="293" width="0.0357%" height="15" fill="rgb(207,160,47)" fg:x="0" fg:w="20"/><text x="0.2500%" y="303.50"></text></g><g><title>`0x7FFD534A0046 (6 samples, 0.01%)</title><rect x="0.0446%" y="277" width="0.0107%" height="15" fill="rgb(228,23,34)" fg:x="25" fg:w="6"/><text x="0.2946%" y="287.50"></text></g><g><title>`0x7FFD5348553D (6 samples, 0.01%)</title><rect x="0.0446%" y="261" width="0.0107%" height="15" fill="rgb(218,30,26)" fg:x="25" fg:w="6"/><text x="0.2946%" y="271.50"></text></g><g><title>`0x7FFD5341423B (6 samples, 0.01%)</title><rect x="0.0446%" y="245" width="0.0107%" height="15" fill="rgb(220,122,19)" fg:x="25" fg:w="6"/><text x="0.2946%" y="255.50"></text></g><g><title>`0x7FFD53476225 (6 samples, 0.01%)</title><rect x="0.0446%" y="229" width="0.0107%" height="15" fill="rgb(250,228,42)" fg:x="25" fg:w="6"/><text x="0.2946%" y="239.50"></text></g><g><title>`0x7FFD53A82AC6 (6 samples, 0.01%)</title><rect x="0.0446%" y="213" width="0.0107%" height="15" fill="rgb(240,193,28)" fg:x="25" fg:w="6"/><text x="0.2946%" y="223.50"></text></g><g><title>`0x7FFD56115D80 (6 samples, 0.01%)</title><rect x="0.0446%" y="197" width="0.0107%" height="15" fill="rgb(216,20,37)" fg:x="25" fg:w="6"/><text x="0.2946%" y="207.50"></text></g><g><title>`0x7FFD560C84E0 (6 samples, 0.01%)</title><rect x="0.0446%" y="181" width="0.0107%" height="15" fill="rgb(206,188,39)" fg:x="25" fg:w="6"/><text x="0.2946%" y="191.50"></text></g><g><title>kaori.exe`alloc::borrow::impl$1::to_owned(enum2$ (7,745 samples, 13.81%)</title><rect x="18.0975%" y="85" width="13.8094%" height="15" fill="rgb(217,207,13)" fg:x="10150" fg:w="7745"/><text x="18.3475%" y="95.50">kaori.exe`alloc::borr..</text></g><g><title>kaori.exe`kaori::backend::vm::value::impl$2::clone(enum2$ (7,745 samples, 13.81%)</title><rect x="18.0975%" y="69" width="13.8094%" height="15" fill="rgb(231,73,38)" fg:x="10150" fg:w="7745"/><text x="18.3475%" y="79.50">kaori.exe`kaori::back..</text></g><g><title>kaori.exe`alloc::vec::Vec&lt;enum2$&lt;kaori::backend::vm::value::Value&gt;,alloc::alloc::Global&gt;::as_slice (177 samples, 0.32%)</title><rect x="31.9069%" y="53" width="0.3156%" height="15" fill="rgb(225,20,46)" fg:x="17895" fg:w="177"/><text x="32.1569%" y="63.50"></text></g><g><title>kaori.exe`alloc::vec::impl$8::deref(alloc::vec::Vec&lt;enum2$ (1,992 samples, 3.55%)</title><rect x="31.9069%" y="69" width="3.5518%" height="15" fill="rgb(210,31,41)" fg:x="17895" fg:w="1992"/><text x="32.1569%" y="79.50">kaor..</text></g><g><title>kaori.exe`core::slice::index::impl$2::index(unsigned long long, ref$&lt;slice2$&lt;enum2$ (1,815 samples, 3.24%)</title><rect x="32.2225%" y="53" width="3.2362%" height="15" fill="rgb(221,200,47)" fg:x="18072" fg:w="1815"/><text x="32.4725%" y="63.50">kao..</text></g><g><title>kaori.exe`alloc::vec::impl$13::index(alloc::vec::Vec&lt;enum2$ (2,513 samples, 4.48%)</title><rect x="31.9069%" y="85" width="4.4807%" height="15" fill="rgb(226,26,5)" fg:x="17895" fg:w="2513"/><text x="32.1569%" y="95.50">kaori..</text></g><g><title>kaori.exe`kaori::backend::vm::value::impl$2::clone(enum2$ (521 samples, 0.93%)</title><rect x="35.4587%" y="69" width="0.9289%" height="15" fill="rgb(249,33,26)" fg:x="19887" fg:w="521"/><text x="35.7087%" y="79.50"></text></g><g><title>kaori.exe`core::ptr::drop_in_place(enum2$ (1,053 samples, 1.88%)</title><rect x="36.3876%" y="85" width="1.8775%" height="15" fill="rgb(235,183,28)" fg:x="20408" fg:w="1053"/><text x="36.6376%" y="95.50">k..</text></g><g><title>kaori.exe`kaori::backend::vm::callstack::Callstack::declare(enum2$ (795 samples, 1.42%)</title><rect x="38.2651%" y="85" width="1.4175%" height="15" fill="rgb(221,5,38)" fg:x="21461" fg:w="795"/><text x="38.5151%" y="95.50"></text></g><g><title>kaori.exe`core::ptr::drop_in_place(enum2$ (15 samples, 0.03%)</title><rect x="39.6559%" y="69" width="0.0267%" height="15" fill="rgb(247,18,42)" fg:x="22241" fg:w="15"/><text x="39.9059%" y="79.50"></text></g><g><title>kaori.exe`kaori::backend::vm::callstack::Callstack::enter_scope (202 samples, 0.36%)</title><rect x="39.6826%" y="85" width="0.3602%" height="15" fill="rgb(241,131,45)" fg:x="22256" fg:w="202"/><text x="39.9326%" y="95.50"></text></g><g><title>kaori.exe`alloc::vec::Vec&lt;usize,alloc::alloc::Global&gt;::push (202 samples, 0.36%)</title><rect x="39.6826%" y="69" width="0.3602%" height="15" fill="rgb(249,31,29)" fg:x="22256" fg:w="202"/><text x="39.9326%" y="79.50"></text></g><g><title>kaori.exe`core::ptr::write (188 samples, 0.34%)</title><rect x="39.7076%" y="53" width="0.3352%" height="15" fill="rgb(225,111,53)" fg:x="22270" fg:w="188"/><text x="39.9576%" y="63.50"></text></g><g><title>kaori.exe`kaori::backend::vm::callstack::Callstack::exit_scope (196 samples, 0.35%)</title><rect x="40.0428%" y="85" width="0.3495%" height="15" fill="rgb(238,160,17)" fg:x="22458" fg:w="196"/><text x="40.2928%" y="95.50"></text></g><g><title>kaori.exe`alloc::vec::Vec&lt;usize,alloc::alloc::Global&gt;::pop (196 samples, 0.35%)</title><rect x="40.0428%" y="69" width="0.3495%" height="15" fill="rgb(214,148,48)" fg:x="22458" fg:w="196"/><text x="40.2928%" y="79.50"></text></g><g><title>kaori.exe`core::slice::impl$0::get_unchecked(ref$&lt;slice2$&lt;enum2$ (976 samples, 1.74%)</title><rect x="40.3923%" y="69" width="1.7402%" height="15" fill="rgb(232,36,49)" fg:x="22654" fg:w="976"/><text x="40.6423%" y="79.50"></text></g><g><title>kaori.exe`core::slice::index::impl$2::get_unchecked(unsigned long long, ptr_const$&lt;slice2$&lt;enum2$ (976 samples, 1.74%)</title><rect x="40.3923%" y="53" width="1.7402%" height="15" fill="rgb(209,103,24)" fg:x="22654" fg:w="976"/><text x="40.6423%" y="63.50"></text></g><g><title>kaori.exe`core::slice::index::get_noubcheck(ptr_const$&lt;slice2$&lt;enum2$ (976 samples, 1.74%)</title><rect x="40.3923%" y="37" width="1.7402%" height="15" fill="rgb(229,88,8)" fg:x="22654" fg:w="976"/><text x="40.6423%" y="47.50"></text></g><g><title>kaori.exe`kaori::backend::vm::callstack::Callstack::load_global (3,943 samples, 7.03%)</title><rect x="40.3923%" y="85" width="7.0304%" height="15" fill="rgb(213,181,19)" fg:x="22654" fg:w="3943"/><text x="40.6423%" y="95.50">kaori.exe..</text></g><g><title>kaori.exe`kaori::backend::vm::value::impl$2::clone(enum2$ (2,967 samples, 5.29%)</title><rect x="42.1325%" y="69" width="5.2902%" height="15" fill="rgb(254,191,54)" fg:x="23630" fg:w="2967"/><text x="42.3825%" y="79.50">kaori...</text></g><g><title>kaori.exe`core::ptr::drop_in_place(enum2$ (509 samples, 0.91%)</title><rect x="51.9426%" y="69" width="0.9076%" height="15" fill="rgb(241,83,37)" fg:x="29132" fg:w="509"/><text x="52.1926%" y="79.50"></text></g><g><title>kaori.exe`kaori::backend::vm::callstack::Callstack::store_global(enum2$ (3,107 samples, 5.54%)</title><rect x="47.4227%" y="85" width="5.5398%" height="15" fill="rgb(233,36,39)" fg:x="26597" fg:w="3107"/><text x="47.6727%" y="95.50">kaori.e..</text></g><g><title>kaori.exe`core::slice::impl$0::get_unchecked_mut(ref_mut$&lt;slice2$&lt;enum2$ (63 samples, 0.11%)</title><rect x="52.8501%" y="69" width="0.1123%" height="15" fill="rgb(226,3,54)" fg:x="29641" fg:w="63"/><text x="53.1001%" y="79.50"></text></g><g><title>kaori.exe`core::slice::index::impl$2::get_unchecked_mut(unsigned long long, ptr_mut$&lt;slice2$&lt;enum2$ (63 samples, 0.11%)</title><rect x="52.8501%" y="53" width="0.1123%" height="15" fill="rgb(245,192,40)" fg:x="29641" fg:w="63"/><text x="53.1001%" y="63.50"></text></g><g><title>kaori.exe`core::slice::index::get_mut_noubcheck(ptr_mut$&lt;slice2$&lt;enum2$ (63 samples, 0.11%)</title><rect x="52.8501%" y="37" width="0.1123%" height="15" fill="rgb(238,167,29)" fg:x="29641" fg:w="63"/><text x="53.1001%" y="47.50"></text></g><g><title>kaori.exe`core::slice::index::impl$2::get_unchecked(unsigned long long, ptr_const$&lt;slice2$&lt;enum2$ (632 samples, 1.13%)</title><rect x="56.6586%" y="53" width="1.1269%" height="15" fill="rgb(232,182,51)" fg:x="31777" fg:w="632"/><text x="56.9086%" y="63.50"></text></g><g><title>kaori.exe`core::slice::index::get_noubcheck(ptr_const$&lt;slice2$&lt;enum2$ (632 samples, 1.13%)</title><rect x="56.6586%" y="37" width="1.1269%" height="15" fill="rgb(231,60,39)" fg:x="31777" fg:w="632"/><text x="56.9086%" y="47.50"></text></g><g><title>kaori.exe`kaori::backend::vm::value_stack::ValueStack::pop (17,905 samples, 31.92%)</title><rect x="52.9625%" y="85" width="31.9248%" height="15" fill="rgb(208,69,12)" fg:x="29704" fg:w="17905"/><text x="53.2125%" y="95.50">kaori.exe`kaori::backend::vm::value_stack::ValueStac..</text></g><g><title>kaori.exe`core::slice::impl$0::get_unchecked(ref$&lt;slice2$&lt;enum2$ (15,832 samples, 28.23%)</title><rect x="56.6586%" y="69" width="28.2286%" height="15" fill="rgb(235,93,37)" fg:x="31777" fg:w="15832"/><text x="56.9086%" y="79.50">kaori.exe`core::slice::impl$0::get_unchecked(..</text></g><g><title>kaori.exe`kaori::backend::vm::value::impl$2::clone(enum2$ (15,200 samples, 27.10%)</title><rect x="57.7855%" y="53" width="27.1017%" height="15" fill="rgb(213,116,39)" fg:x="32409" fg:w="15200"/><text x="58.0355%" y="63.50">kaori.exe`kaori::backend::vm::value::impl$2..</text></g><g><title>kaori.exe`core::ptr::drop_in_place(enum2$ (631 samples, 1.13%)</title><rect x="94.2373%" y="69" width="1.1251%" height="15" fill="rgb(222,207,29)" fg:x="52853" fg:w="631"/><text x="94.4873%" y="79.50"></text></g><g><title>kaori.exe`kaori::run_program (56,052 samples, 99.94%)</title><rect x="0.0571%" y="117" width="99.9412%" height="15" fill="rgb(206,96,30)" fg:x="32" fg:w="56052"/><text x="0.3071%" y="127.50">kaori.exe`kaori::run_program</text></g><g><title>kaori.exe`kaori::backend::vm::kaori_vm::KaoriVM::execute_instructions (56,050 samples, 99.94%)</title><rect x="0.0606%" y="101" width="99.9376%" height="15" fill="rgb(218,138,4)" fg:x="34" fg:w="56050"/><text x="0.3106%" y="111.50">kaori.exe`kaori::backend::vm::kaori_vm::KaoriVM::execute_instructions</text></g><g><title>kaori.exe`kaori::backend::vm::value_stack::ValueStack::push(enum2$ (8,475 samples, 15.11%)</title><rect x="84.8872%" y="85" width="15.1110%" height="15" fill="rgb(250,191,14)" fg:x="47609" fg:w="8475"/><text x="85.1372%" y="95.50">kaori.exe`kaori::backen..</text></g><g><title>kaori.exe`core::slice::impl$0::get_unchecked_mut(ref_mut$&lt;slice2$&lt;enum2$ (2,600 samples, 4.64%)</title><rect x="95.3624%" y="69" width="4.6358%" height="15" fill="rgb(239,60,40)" fg:x="53484" fg:w="2600"/><text x="95.6124%" y="79.50">kaori..</text></g><g><title>kaori.exe`core::slice::index::impl$2::get_unchecked_mut(unsigned long long, ptr_mut$&lt;slice2$&lt;enum2$ (2,600 samples, 4.64%)</title><rect x="95.3624%" y="53" width="4.6358%" height="15" fill="rgb(206,27,48)" fg:x="53484" fg:w="2600"/><text x="95.6124%" y="63.50">kaori..</text></g><g><title>kaori.exe`core::slice::index::get_mut_noubcheck(ptr_mut$&lt;slice2$&lt;enum2$ (2,600 samples, 4.64%)</title><rect x="95.3624%" y="37" width="4.6358%" height="15" fill="rgb(225,35,8)" fg:x="53484" fg:w="2600"/><text x="95.6124%" y="47.50">kaori..</text></g><g><title>all (56,085 samples, 100%)</title><rect x="0.0000%" y="341" width="100.0000%" height="15" fill="rgb(250,213,24)" fg:x="0" fg:w="56085"/><text x="0.2500%" y="351.50"></text></g><g><title>`0x7FFD560FC34C (56,064 samples, 99.96%)</title><rect x="0.0374%" y="325" width="99.9626%" height="15" fill="rgb(247,123,22)" fg:x="21" fg:w="56064"/><text x="0.2874%" y="335.50">`0x7FFD560FC34C</text></g><g><title>`0x7FFD553DE8D7 (56,064 samples, 99.96%)</title><rect x="0.0374%" y="309" width="99.9626%" height="15" fill="rgb(231,138,38)" fg:x="21" fg:w="56064"/><text x="0.2874%" y="319.50">`0x7FFD553DE8D7</text></g><g><title>kaori.exe`__scrt_common_main_seh() (56,060 samples, 99.96%)</title><rect x="0.0446%" y="293" width="99.9554%" height="15" fill="rgb(231,145,46)" fg:x="25" fg:w="56060"/><text x="0.2946%" y="303.50">kaori.exe`__scrt_common_main_seh()</text></g><g><title>kaori.exe`invoke_main() (56,053 samples, 99.94%)</title><rect x="0.0571%" y="277" width="99.9429%" height="15" fill="rgb(251,118,11)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="287.50">kaori.exe`invoke_main()</text></g><g><title>kaori.exe`main (56,053 samples, 99.94%)</title><rect x="0.0571%" y="261" width="99.9429%" height="15" fill="rgb(217,147,25)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="271.50">kaori.exe`main</text></g><g><title>kaori.exe`std::rt::lang_start_internal (56,053 samples, 99.94%)</title><rect x="0.0571%" y="245" width="99.9429%" height="15" fill="rgb(247,81,37)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="255.50">kaori.exe`std::rt::lang_start_internal</text></g><g><title>kaori.exe`std::panic::catch_unwind (56,053 samples, 99.94%)</title><rect x="0.0571%" y="229" width="99.9429%" height="15" fill="rgb(209,12,38)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="239.50">kaori.exe`std::panic::catch_unwind</text></g><g><title>kaori.exe`std::panicking::try (56,053 samples, 99.94%)</title><rect x="0.0571%" y="213" width="99.9429%" height="15" fill="rgb(227,1,9)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="223.50">kaori.exe`std::panicking::try</text></g><g><title>kaori.exe`std::panicking::try::do_call (56,053 samples, 99.94%)</title><rect x="0.0571%" y="197" width="99.9429%" height="15" fill="rgb(248,47,43)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="207.50">kaori.exe`std::panicking::try::do_call</text></g><g><title>kaori.exe`std::rt::lang_start_internal::closure$0 (56,053 samples, 99.94%)</title><rect x="0.0571%" y="181" width="99.9429%" height="15" fill="rgb(221,10,30)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="191.50">kaori.exe`std::rt::lang_start_internal::closure$0</text></g><g><title>kaori.exe`std::rt::lang_start::closure$0&lt;tuple$&lt;&gt; &gt;(std::rt::lang_start::closure_env$0&lt;tuple$ (56,053 samples, 99.94%)</title><rect x="0.0571%" y="165" width="99.9429%" height="15" fill="rgb(210,229,1)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="175.50">kaori.exe`std::rt::lang_start::closure$0&lt;tuple$&lt;&gt; &gt;(std::rt::lang_start::closure_env$0&lt;tuple$</text></g><g><title>kaori.exe`std::sys::backtrace::__rust_begin_short_backtrace&lt;void (*)(),tuple$&lt;&gt; &gt;(void (*) (56,053 samples, 99.94%)</title><rect x="0.0571%" y="149" width="99.9429%" height="15" fill="rgb(222,148,37)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="159.50">kaori.exe`std::sys::backtrace::__rust_begin_short_backtrace&lt;void (*)(),tuple$&lt;&gt; &gt;(void (*)</text></g><g><title>kaori.exe`kaori::main (56,053 samples, 99.94%)</title><rect x="0.0571%" y="133" width="99.9429%" height="15" fill="rgb(234,67,33)" fg:x="32" fg:w="56053"/><text x="0.3071%" y="143.50">kaori.exe`kaori::main</text></g></svg></svg>