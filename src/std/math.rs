/* use crate::runtime::value::Value;

pub fn native_sin(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.sin())
}

pub fn native_cos(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.cos())
}

pub fn native_tan(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.tan())
}

pub fn native_sqrt(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.sqrt())
}

pub fn native_abs(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.abs())
}

pub fn native_floor(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.floor())
}

pub fn native_ceil(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.ceil())
}

pub fn native_round(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.round())
}

pub fn native_pow(args: &[Value]) -> Value {
    let x = args[0].as_number();
    let y = args[1].as_number();

    Value::number(x.powf(y))
}

pub fn native_log(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.ln())
}

pub fn native_log2(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.log2())
}

pub fn native_log10(args: &[Value]) -> Value {
    let x = args[0].as_number();

    Value::number(x.log10())
}

pub fn native_min(args: &[Value]) -> Value {
    let x = args[0].as_number();
    let y = args[1].as_number();

    Value::number(x.min(y))
}

pub fn native_max(args: &[Value]) -> Value {
    let x = args[0].as_number();
    let y = args[1].as_number();

    Value::number(x.max(y))
}

pub fn native_clamp(args: &[Value]) -> Value {
    let x = args[0].as_number();
    let min = args[1].as_number();
    let max = args[2].as_number();

    Value::number(x.clamp(min, max))
}

pub fn native_pi(_args: &[Value]) -> Value {
    Value::number(std::f64::consts::PI)
}

pub fn native_e(_args: &[Value]) -> Value {
    Value::number(std::f64::consts::E)
}
 */
