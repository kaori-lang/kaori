def fib(n: number) -> bool {
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }   
}


def main()  {
    $n: number = 5;

    print(fib(5) + true);
    //fib(40);
}
