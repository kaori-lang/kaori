fun foo(n: number) {
    print(n);
    
    if n > 0 {
        bar(n - 1);
    }

}

fun bar(n: number) {
    print(n);

    if n > 0 {
        foo(n - 1);
    }

}


def main() {
    print(foo(10));
}