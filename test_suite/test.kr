fun Cat(name, age, color) {
    return {   
        name,
        age,
        color,
    };
}

fun main() {
    cat := Cat("meow", 5, "black");

    sum := 0;
    N := 100000000;

    for i := 0; i < N; i += 1 {
        sum += cat.age;
    }

}