
iterations := 1000000;
max_fib := 30;
i := 0;



while i < iterations {
    a := 0;
    b := 1;
    j := 0;
    
    while j < max_fib {
        temp := a + b;
        a = b;
        b = temp;
        j += 1;
    }

    i += 1;

}
