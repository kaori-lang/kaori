/*
fun Cat(name, age, color) {
    {   
        name,
        age,
        color
        get_name
    }
}

fun get_name(self) {
    return self.name;
}

fun main()  {
    let cat = Cat("meow", 5, "black");
    
    cat.get_name(cat);

}
*/

fun Cat(name, age, color) {
    return {   
        name,
        age,
        color,
    };
}

fun greet(self) {
    return self.name;
}

fun main()  {
    let cat = Cat("meow", 5, "black");
    print(greet(cat));
}





