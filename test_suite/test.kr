fun main() {
    iterations := 1000000;
    max_fib := 30;

    for i := 0; i < iterations; i += 1 {
        a := 0;
        b := 1;

        for j := 0; j < max_fib; j += 1 {
            temp := a + b;
            a = b;
            b = temp;
        }
    }
}


/*
0000  LOAD_IMM r1 0
0001  MOV r0 r1
0002  LT r2 r0 100
0003  JMP_IF_FALSE r2 6
0004  PRINT r0
0005  ADD r3 r0 1
0006  MOV r0 r3
0007  LT r4 r0 100
0008  JMP_IF_TRUE r4 -4
0009  LOAD_IMM r5 0
0010  RET r5

fn main() {
    x := 0; // r0
    y := 0; // r1

    z := foo(a, b) {
        a // r0
        b // r1
        x // r2
        z // r3

        w := bar(c, d) {
            c // r0
            d // r1
            a // r2
            b // r3
            x // r4
            z // r5

            e := 7; // r6

            print(x); // r4

        }
        print(x) // r2
        r := 11; // r4
    }

    s := 10; // r2

}*/