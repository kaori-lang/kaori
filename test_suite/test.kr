
fun fib(n) {
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }   
}

fun main()  {
    fib(30);
}






/*
fn main() {
    x := 0; // r0
    y := 0; // r1

    z := foo(a, b) {
        a // r0
        b // r1
        x // r2
        z // r3

        w := bar(c, d) {
            c // r0
            d // r1
            a // r2
            b // r3
            x // r4
            z // r5

            e := 7; // r6

            print(x); // r4

        }
        print(x) // r2
        r := 11; // r4
    }

    s := 10; // r2

}*/