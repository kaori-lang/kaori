
fun Cat(name, age, color) {
    return {   
        name,
        age,
        color,
    };
}

fun main() {
    cat := Cat("meow", 5, "black");

    sum := 0;

    for i := 0; i < 100000000; i += 1 {
        sum += cat.age;
    }

    print(sum);
}