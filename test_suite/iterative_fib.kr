def main() -> number  {
    $iterations: number = 1000000;
    $max_fib: number = 40;

    for $i: number = 0; i < iterations; i += 1 {
        $a: number = 0;
        $b: number = 1;

        for $j: number = 0; j < max_fib; j += 1 {
            $temp: number = a + b;
            a = b;
            b = temp;
        }

    }

    return 1;
}

