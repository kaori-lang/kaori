def main() -> number  {
    var iterations: number = 1000000;
    var max_fib: number = 40;

    for var i: number = 0; i < iterations; i += 1 {
        var a: number = 0;
        var b: number = 1;

        for var j: number = 0; j < max_fib; j += 1 {
            var temp: number = a + b;
            a = b;
            b = temp;
        }

    }

    return 1;
}

